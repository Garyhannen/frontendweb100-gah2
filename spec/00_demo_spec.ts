describe('writing a Spec', () => {
    it('is easey when you know how', () => {
        // given 
        const a = 10, b = 20;
        // when
        const answer = a + b;
        //then - 
        expect(answer).toBe(30);
    });

});